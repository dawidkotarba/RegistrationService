allprojects {
    ext {
        // Java
        sourceCompatibility = 1.8
        targetCompatibility = 1.8

        // Spring
        activeProfile = "prod"

        // Eureka Service Discovery
        eurekaIpAddress = "localhost"
        eurekaServerPort = "8080"

        // OAuth2 token endpoint
        oauthServerIpAddress = "localhost"
        oauthServerPort = "8081"
    }
}

project(":ServiceDiscoveryModule") {
    ext {
        applicationName = "Service_Discovery_Module"
        serverPort = eurekaServerPort
        jmxServerPort = "9090"
    }
}

project(":AuthenticationModule") {
    ext {
        applicationName = "Authentication_Module"
        serverPort = oauthServerPort
        jmxServerPort = "9091"
    }
}

project(":UsersModule") {
    ext {
        applicationName = "Users_Module"
        serverPort = "8082"
        jmxServerPort = "9092"
    }
}