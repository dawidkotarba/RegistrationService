apply plugin: "spring-boot"

jar {
    baseName = "TemplateModule"
    version = "0.0.1-SNAPSHOT"
}

dependencies {
    compile project(':SharedModule')
}

springBoot {
    mainClass = "dawid.kotarba.template.TemplateModule"
}